# research/

Raw notes from exploring docs, blog posts, and repos before they get distilled into `shared/` or `claude/`.

- `anthropic/` — anthropic.com docs and the `anthropics` GitHub org.

Add a new subdir per source (e.g. `cursor/`, `community/`) as the exploration widens.
